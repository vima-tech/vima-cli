# vima-cli 工程规范（v4）

## 使命

vima 做三件事：**定形**（规定合法产出长什么样）· **供料**（提供现成的块与经验）·
**验形**（记录并呈现，保证前两件真的发生）。

前两件直接解两个痛点——AI 生成的东西风格多变、做过的东西下次用不上。
第三件保证前两件不落空：没有它，前两件会在「AI 说做到了」那一步悄悄失效。

**对齐真源是仓库根的 `ARCHITECTURE.md`。改模块接口前先改它。**
它写明了分层、三个概念、两条强度轴、四条决定性纪律、以及 Claude Code 资产面的签名。

设计依据是需求基线 R1–R11 与约束 C1–C4。**正本在 `docs/requirements-baseline.md`**
（含每条的**判据**、裁定、边界、实证与欠账明细）；`ARCHITECTURE.md` 的基线表是它的索引。
两处冲突以正本为准。反查一条需求时看判据那一列——「怎么算满足」才是可核的部分，
只看「要点」核不出任何东西。

`docs/v3-archive/` 是 v3 的设计册与实测记录。**那套实现已整体删除，与 v4 没有共同代码**，
照着做会做出一个不存在的产品。留着是为了保住「当时为什么这么定」，不是为了参考方案。

## 硬约束

- **零运行时依赖**：只用 Node.js ≥20 内建模块（node:fs、node:path、node:util.parseArgs、
  node:crypto、node:readline 等）。不引入任何 npm 依赖，devDependencies 也不加。
- **确定性优先**：凡能用文件系统 + 确定性代码解决的（校验、拓扑、渲染、对账），
  不留给 Agent 概率性行为。同一输入必须字节一致——不嵌时间戳、不用随机数。
  这条也管闸门：**不用 LLM 判定的 hook**（`type: "prompt"`）。
- **原子写**：写文件一律 `core/fsx.atomicWrite`（临时文件 + rename）。
- **事件是唯一写入口**：状态改变一律走 `core/events.append`，append-only，无 update 无 delete。
  时间戳由它按注入的 `now` 打——`ops/` 整层不碰时钟，这样才干跑得起来。
- **错误输出稳定**：走 `front/actions.FrontError`，stderr 格式
  `vima <cmd>: <CODE>: <message>`。退出码见 `ARCHITECTURE.md`，刻意少：
  每个码对应一类**处置方式不同**的失败，处置一样的不分家。
- **分层边界**：依赖方向单向向下 `front → ops/assets → core`，`core/` 不 import 上层，
  `ops/` 与 `assets/` 是兄弟层互不 import（需要对方的结果就由 `front/` 注入进 ctx）。
  Claude Code 语义只存在于 `templates/project/` 与 `lib/front/claude.mjs`——
  后者是安装器/投影侧，是这条边界唯一的、写明了的例外。
- **判据只有一处**：同一件事的判定逻辑不许有第二份实现。
  代码事实只从 `core/extract.mjs` 提取；端点键只由 `endpointKey` 算；
  死规则只由 `assets/rules.deadRules` 判；`.mcp.json` 只由 `front/claude.mcpConfig` 生成。
  上一代在这件事上连续漂移三次，每次都是同一个病换个位置。
- **防过度设计**：每处实现必须能反查到 R# 或 C#；反查不到的不写。
- **立项即做透**：需求一旦立项，就按长远正确的形态一次做完整——数据结构留足字段、
  消费端一并接线、契约与校验同步落位。不交半截实现：块定义了没人消费、字段加了校验不覆盖、
  文档改了测试没跟，都算夹生。与「防过度设计」的分工：前者管**广度**（没立项的不做），
  本条管**深度**（已立项的不做夹生）。判据顺序：先问「这是哪条需求要的」，
  反查不到就不做；反查得到就做透，此时「改动面大」不构成降级理由。

## 测试纪律

这个仓库反复吃亏的不是「代码写错了」，是**接缝**——两个正确的部件按各自正确的方式
做完，合起来不通，而两边的测试都绿。所以：

- **消费方的契约不许在生产方的测试里复述。** 抄一份「允许键清单」出来，抄的那份就是
  第二个真源。改成让真消费者去判：它不抛，就是对齐的。
- **凡是「A 产出的东西要被 B 吃掉」的地方，`tests/unit/z.seams.test.mjs` 必须有一条**，
  且用真文件真函数，不许造夹具。造夹具就是把假设又复制了一份。
- **不喂合成对象。** 写盘读回走一遍真实链路——合成投影会掩盖 `lastTouched` 那类 bug。
- **新写的检查要做一次变异验证**：把被测的东西改坏，确认它真的会红。
  一条只会报绿的检查比没有检查更糟。
- **静默为空不算通过。** 扫不到东西时要明说「这一侧没判」，不能计为 0 个问题。

## 验证

```bash
npm test          # node --test tests/unit/*.test.mjs
npm run smoke     # 端到端冒烟，走真实 CLI 进程（import 内部函数会绕过参数解析与退出码）
node bin/vima.mjs help
```

## 纪律

- 不执行真实 npm publish / git push。
- 完成编辑即停手，不主动 git commit（用户明确要求时才提交）。
- 文档里的每条命令、每个字段都要能反查到实现。**产品对使用者撒谎，
  和 agent 自称完成是同一种病**——README 里的命令列表有测试盯着（见 z.seams）。
