{
  "id": "h5",
  "name": "移动端 H5",
  "status": "preview",
  "description": "已由 admin 模板的 h5-mobile kind 取代（A25）：vima create <name> -t admin --apps <id>:h5-mobile，或在已有项目里 vima app add <id> --kind h5-mobile。本模板保留为占位，不再单独维护骨架。",
  "version": "2.0.0",
  "techStack": {
    "frontend": "Vue 3 + Vite + vendored @vima-tech/ui-h5（骨架在 admin 模板的 scaffold/h5-mobile）"
  },
  "prerequisites": [
    {
      "tool": "node",
      "check": "node --version",
      "constraint": ">=20"
    }
  ]
}
