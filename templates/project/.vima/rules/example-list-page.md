---
layer: impl
side: admin
app: admin
block: [list]
---

# 列表页必须有空状态

本项目的列表页在无数据时必须渲染空状态块，不允许只留一张空表头。

<!--
  这是一个**示例文件**，用来说明项目特有规则长什么样。不适用就删掉它。

  frontmatter 只允许四个维度键：**layer / side / app / block**（block 单数）。
  写错键会当场抛 RULE_UNKNOWN_KEY——刻意不容忍，因为被静默忽略的 `sides:`
  会让一条规则从「只在小程序生效」悄悄变成「处处生效」。
  写了哪个维度就只在该维度匹配时命中；省略 = 该维度不限。

  **没有 id 字段**——规则 id 从文件名推导。同一件事两个真源，必然有对不上的一天。
  值只能是标量或行内数组（`[a, b]`）——解析器是 core/fsx.mjs 的极简 frontmatter，
  不支持嵌套对象和多行数组。

  什么该写在这里：
    - 只对**这个项目**成立的约束。通用的写进资产仓 assets/rules/，别在这里重复一份。
    - 能被指出来的（机检或人审都行）。写不出「违反长什么样」的，不是规则，是愿望。

  什么不该写在这里：
    - 能做成令牌/词表的（写不出非法的东西，比事后指出更强）。
    - 长规程（那是 skill）。规则是一句判据，不是一套步骤。

  规则从未被任何任务命中 = 死规则。`vima audit` 会报出来，
  到时候要么补覆盖面，要么删掉——留着不命中的规则会稀释整张规则表的可信度。
-->
