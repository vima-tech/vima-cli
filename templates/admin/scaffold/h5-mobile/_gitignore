node_modules/
dist/
# 上一行只针对**构建产物**。vendor/ 下的 dist 是入库的组件库本体，不能被它连坐——
# README 写明 vendored 组件库「保证离线可用与版本锁定」，package.json 的
# `file:./vendor/...` 依赖直接指向它。被忽略的后果是：克隆下来 npm install
# 装到一个只有 package.json 的空包，前端根本构建不起来（Sustain 实测中招，
# 61 个 dist 文件全部游离在版本库外，连 PATCHES.md 记录的本地补丁也无处落地）。
!**/vendor/*/dist/
.DS_Store
*.local
